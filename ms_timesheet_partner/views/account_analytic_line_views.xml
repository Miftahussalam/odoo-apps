<odoo>
    <data>

        <!-- Inherit Form View to Modify it -->
        <record id="hr_timesheet_line_search" model="ir.ui.view">
            <field name="name">account.analytic.line.search</field>
            <field name="model">account.analytic.line</field>
            <field name="inherit_id" ref="hr_timesheet.hr_timesheet_line_search"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='project_id']" position="after">
                    <field name="partner_id" string="Customer"/>
                </xpath>
                <xpath expr="//filter[@name='groupby_project']" position="after">
                    <filter string="Customer" name="groupby_customer" domain="[]" context="{'group_by': 'partner_id'}" help="Timesheet by Customer"/>
                </xpath>

            </field>
        </record>

        <record id="hr_timesheet.act_hr_timesheet_line" model="ir.actions.act_window">
            <field name="domain">[]</field>
        </record>

        <record id="hr_timesheet.timesheet_menu_root" model="ir.ui.menu">
            <field name="groups_id" eval="[(4, ref('ms_timesheet_partner.group_hr_timesheet_partner'))]"/>
        </record>

    </data>
</odoo>